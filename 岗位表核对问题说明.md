# 岗位表核对问题说明

这份说明用于配合“岗位表核对”功能导出的结果表，帮助人工快速理解每一种问题的含义。

## 编码规则

岗位表编码总长度固定为 10 位，结构如下：

`01 001 001 01`

各段含义：

- `01`：地市代码
- `001`：主管部门编码
- `001`：报考单位编码
- `01`：报考岗位编码

上下级关系如下：

- 地市
- 主管部门
- 报考单位
- 报考岗位

顺延规则如下：

- 每个地市下面的主管部门编码，都要从 `001` 重新开始
- 每个主管部门下面的报考单位编码，都要从 `001` 重新开始
- 每个报考单位下面的报考岗位编码，都要从 `01` 重新开始
- 主管部门、报考单位、报考岗位名称可以为空，但空名称也占一个编码位

## 导出文件说明

导出后会得到两类文件：

- `岗位核对总表.xlsx`
  用于总览全部地市的问题
- 按地市拆分的多个 `xlsx`
  每个地市一个文件，便于分发和核对

其中：

- 总表包含 `校验摘要` 和 `问题明细`
- 分地市文件只保留 `问题明细`
- `问题类型` 一列会标红，方便快速定位重点问题

## 问题类型说明

### 1. 缺少必填值

含义：

- 这一行有关键字段没有填写，无法正常判断层级和编码关系

常见情况：

- 地市为空
- 地市代码为空
- 主管部门编码为空
- 报考单位编码为空
- 报考岗位编码为空

处理建议：

- 回到原始岗位表，把缺失内容补齐后重新核对

### 2. 编码格式错误

含义：

- 编码位数不对，或者不是纯数字

正确格式：

- 地市代码必须是 2 位数字
- 主管部门编码必须是 3 位数字
- 报考单位编码必须是 3 位数字
- 报考岗位编码必须是 2 位数字

常见情况：

- 把 `1` 写成了 `1` 而不是 `001`
- 把 `A01`、`01-1`、`3.0` 这类值填进编码列

处理建议：

- 统一改成固定长度的纯数字编码

### 3. 编码映射冲突

含义：

- 同一个编码，对应了多个不同名称

举例：

- 同一个主管部门编码，既对应“教育局”，又对应“人社局”
- 同一个报考单位编码，对应了两个不同单位名称

这说明什么：

- 编码已经失去唯一性，后续统计、排序、归类都会出错

处理建议：

- 确认到底哪个名称是正确的
- 保证同一个编码只对应一个名称

### 4. 名称映射冲突

含义：

- 同一个名称，对应了多个不同编码

举例：

- 同一个报考单位名称，被分配了两个不同的单位编码
- 同一个岗位名称，在同一层级下用了多个岗位编码

这说明什么：

- 同一个对象被重复编号，容易造成重复统计或顺延错误

处理建议：

- 确认该名称应保留哪个编码
- 保证同一个名称只使用一个编码

### 5. 顺延起始错误

含义：

- 当前父级下面的子级编码，没有从规定的起始值开始

正确起始值：

- 主管部门从 `001` 开始
- 报考单位从 `001` 开始
- 报考岗位从 `01` 开始

举例：

- 某个地市下面主管部门直接从 `002` 开始
- 某个主管部门下面单位直接从 `005` 开始
- 某个单位下面岗位直接从 `03` 开始

处理建议：

- 按当前父级重新整理顺序，从起始值重新编号

### 6. 顺延缺号

含义：

- 当前父级下面的编码虽然有排序，但中间断号了

举例：

- 主管部门编码有 `001、002、004`
- 报考单位编码有 `001、003`
- 报考岗位编码有 `01、02、04`

这说明什么：

- 当前层级的编号不连续

处理建议：

- 检查是否遗漏了某条数据
- 如果不是遗漏，就把编码重新顺延调整为连续编号

## 问题说明列怎么看

结果表里的 `问题说明`，会用通俗文字直接写出问题，比如：

- 哪个编码对应了多个名称
- 哪个名称对应了多个编码
- 哪一层没有从 `001` 或 `01` 开始
- 缺了哪些号码

建议核对顺序：

1. 先看 `问题类型`，快速判断是哪一类问题
2. 再看 `地市 -> 主管部门 -> 报考单位 -> 报考岗位`
3. 最后看 `问题说明`，确认具体应该怎么改

## 推荐核对方式

建议按下面顺序处理：

1. 先处理 `缺少必填值`
2. 再处理 `编码格式错误`
3. 再处理 `编码映射冲突` 和 `名称映射冲突`
4. 最后处理 `顺延起始错误` 和 `顺延缺号`

这样做的原因是：

- 前面的基础问题不修，后面的顺延检查往往也会跟着错

## 适合发给同事的话术

可以直接这样说：

“岗位表核对结果里，先看标红的‘问题类型’，再看最后一列‘问题说明’。总表是全量问题，分地市文件是各地市自己的问题清单。修改原始岗位表后，再重新导入核对，直到没有问题为止。”
